# Dependencies
node_modules/

# Build output (per-plugin)
# main.js and manifest.json ARE committed so users can install without building.
# Only ignore map files and temp artifacts.
*.js.map
*.d.ts.map

# OS / editor
.DS_Store
Thumbs.db
.vscode/settings.json
.references/
